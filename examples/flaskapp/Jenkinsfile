
pipeline {
    agent {
        docker { image 'node:16-alphine' }
    }
    
    stages {
        stage ('Echo Version') {
            steps {
                sh 'node --version'
            }
        }
        
        post {
            success {
                echo "build succeeded"
            }
        }
    }
}
