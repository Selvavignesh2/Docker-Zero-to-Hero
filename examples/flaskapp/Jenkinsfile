pipeline {
    agent {
        docker { image 'node:16-alphine' }
    }
    
    stages {
        stage ('Version') {
            steps {
                sh 'node --version'
            }
        }
    }
}
